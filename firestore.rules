rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir acesso a todas as coleções (temporário para desenvolvimento)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}